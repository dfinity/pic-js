# Google Search

This example demonstrates how to mock HTTPS Outcalls in canisters.

Build the canister:

```shell
pnpm build:examples -- google_search
```

Run the tests:

```shell
pnpm test:examples -- google_search
```
