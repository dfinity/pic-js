# HTTP

This example demonstrates how to use "live" mode with an HTTP canister.

Build the canister:

```shell
pnpm build:examples -- http
```

Run the tests:

```shell
pnpm test:examples -- http
```
